package p2p

// Peer is an interface that represents a remote node in the network
type Peer interface {}

// Transport is an interface that handles any communication between nodes
type Transport interface {}
